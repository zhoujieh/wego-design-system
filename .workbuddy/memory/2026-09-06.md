# 2026-09-06

## 工作流维护：.workbuddy 目录纳管（已完成合入 main）
- 决策：`.workbuddy/` 全量纳入版本管理（skills 符号链接 + memory 备忘），下线 `.codebuddy/skills`；AGENTS.md 符号链接规则改为 `.trae` 与 `.workbuddy`；守门脚本 `validate-wego-design-core.mjs` 适配器清单同步改 `.workbuddy/skills`（否则 gate 报 adapter_missing）。
- 落地：PR #188 已合入 main（51cee57b），分支/远端已清理。
- 注意：`.workbuddy/memory` 此后受 git 跟踪——切分支会带走/恢复该目录，会话结束前若有 memory 改动需打包提交，避免跨会话残留。

## 报价单项目（shop244 导出报价单）阶段E 分享导出
- 项目定位：`wego-app/scenes/shop/导出报价单/`，routeId=quote-export；交付单元 worktree `.tasks/worktrees/export-quote`（分支 feat-add-export-quote-UzxOaG）；进度权威文档 `_iterations/shop244-导出报价单-20260904/阶段推进文档.md`（找项目先查 `git worktree list` 与 `.tasks/`，勿只搜主 worktree）。
- 本日完成阶段E：分享导出（Excel/PDF 浏览器端真实生成+直接下载；图片格式与系统分享按用户约定裁剪）。checkpoint 9e30768c。契约/运行时校验通过，playwright 走查全绿。
- 阶段状态：A 已验收；B/C/D 实现完待用户验收；E 实现完待用户验收；F（报价记录）、G（收口）未开始。
- 预览服务端口 8788（.tasks/preview-servers/export-quote.json）。
- 新踩坑已登记阶段推进文档：跨闭包函数引用（cleanupPreview is not defined）、正则字面引号让契约掩码错位、canvas 取色须运行时读 Token。
- 阶段E 已推送：rebase 远端新提交 d6a1d37a 后推送成功，checkpoint 6955b2ee，PR #175 已更新，在线预览 https://zhoujieh.github.io/wego-design-system/previews/pr-175/ （PR 预览路径规则 previews/pr-<编号>/）。
- 用户反馈线上预览点分享报价单仍出旧占位提示：根因是 GitHub Pages 对场景资源 max-age=600 的浏览器缓存，线上代码实际已是新版；已推修复 d4224611（app.js 在 github.io 环境对场景 script/style 加载时间戳穿透缓存，本地不受影响），用户需强刷一次后后续推送自动生效。
- 用户反馈二次调整：分享报价单不再弹格式选择弹层，直接按预览底栏已选格式（Excel/PDF 分段切换）生成导出；格式弹层代码与样式已删除，checkpoint d06cfd71 已推送（期间远端被并行会话合入 main，rebase 同步）。走查：底栏 PDF→.pdf、Excel→.xls、完成返回全绿。
- 目录纳管扩展：.trae/.codex/.workbuddy 内容全部入库（PR #190 已合入，b726e36c）；.gitignore 移除 .trae/documents、.codebuddy/memory、.codex 迁移标记规则，仅保留 .DS_Store 系统垃圾忽略；.codebuddy 目录本地已不存在，无需处理。
- Excel 导出缺陷修复（用户反馈下载文件 macOS 预览报「无法预览/已损坏」）：根因是 SpreadsheetML 2003 XML 伪装 .xls 不被 Quick Look 识别。已改为纯浏览器端真实 .xlsx 生成器（自写 ZIP/STORE+CRC32 + Open XML parts，无外部依赖），提交 dd51d342。版式对齐用户参考 xlsx：列 序号/商品图/价格/规格/货号/商品名；标题 A1:D1、日期 A2:D2、合计 E1:F2 合并；表头 FFF6F7F8 底+FFEDEFF3 边框；商品图 414x552 JPEG 内嵌 xl/media 并 twoCellAnchor 锚定 B 列；隐藏网格线；价格格 ¥+\n+$ 双行。quoteExportExcelBlob 改为返回 Promise（图片异步+进度）。验证链：契约/运行时校验 → playwright E2E 真实下载 → zipfile/openpyxl 交叉验证全通过。PDF 用户确认正常未动。
- xlsx 修复已推送：远端分支被并行会话 merge main（4a0453bf），本地 merge 同名远端分支后推送 8f210b55；PR #175 已更新，~135s 部署后 curl 校验线上 previews/pr-175/ scene.js 含 quoteXlsxBuild 标记，在线预览 https://zhoujieh.github.io/wego-design-system/previews/pr-175/
- 本日完成阶段F 报价记录：QuoteRecord 契约落 localStorage（wego.quote.records）；预览退出 dirty → dialog「是否保存当前修改？」；记录页全屏 modal（搜索/删除模式+确认弹窗/两空态/卡片=前3缩略图+项数+总价+更新时间）；记录点开载入预览编辑（recordRestore 现场恢复）；切语言计 dirty。checkpoint 9626f0b7（本地未推送）。契约/运行时校验通过，走查脚本 tmp/quote-records-walkthrough.js 25 项全绿。
- 坑：.tasks/preview-servers/export-quote.json 记录过期（8097 被其它项目占用），本项目实际跑 8788，已修正 json；走查 playwright 可直接用主仓 node_modules（tmp/ 下脚本自动解析）。
- 阶段状态：A 已验收；B/C/D/E/F 实现完待用户验收；G（全量走查+confirm-brief+PR+经验收口）未开始，PR 勿自动触发。
- 用户要求清理本地服务：已停掉 3 个遗留预览（8097 同 worktree 过期重复、8701/8712 主仓遗留无记录），保留 8788（export-quote 验收预览）；weux-agent/openclaw 等非本项目进程未动。
