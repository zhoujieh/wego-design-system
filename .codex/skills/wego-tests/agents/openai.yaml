interface:
  display_name: "Wego Tests"
  short_description: "验收产品原型、目录结构、设计消费结果与部署准备状态"
  default_prompt: "Use $wego-tests to validate the final prototype against the page_spec, design_consumption_plan, design rules, task-folder rules, and deployment readiness, then produce an acceptance_report."
