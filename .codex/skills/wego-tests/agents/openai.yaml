interface:
  display_name: "Wego Tests"
  short_description: "验收已注册场景的需求、设计、实现、浏览器交互与交付一致性并输出完整报告"
  default_prompt: "Use $wego-tests to validate this registered wego-app scene against both specs, formal rule sources, browser behavior, and delivery readiness."

policy:
  allow_implicit_invocation: true
