interface:
  display_name: "Wego Design"
  short_description: "消费微购设计系统，产出可追溯 design_plan"
  default_prompt: "Use $wego-design to consume the local design system for this confirmed interaction_spec and produce a gap-free design_plan."

policy:
  allow_implicit_invocation: true
