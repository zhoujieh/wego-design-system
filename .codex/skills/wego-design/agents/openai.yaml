interface:
  display_name: "Wego Design"
  short_description: "基于确认简报生成可交互微购 App 场景"
  default_prompt: "Use $wego-design to consume the confirmed prototype brief, derive the design intent and semantic page structure before Preview-first component mapping, implement the declared wego-app scene, complete one design self-review, and produce traceable design decisions."
