interface:
  display_name: "Wego Design"
  short_description: "基于确认简报生成可交互微购 App 场景"
  default_prompt: "Use $wego-design to consume the confirmed prototype brief, build a preview-first prompt contract, implement the declared wego-app scene, and produce verified design decisions."
