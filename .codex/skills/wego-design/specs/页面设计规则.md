# 页面设计规则

本文档由系统规则自动生成，用于人工检查。请勿直接修改；如需调整规则，应修改对应权威来源后重新生成。

## 什么时候使用

需求规格已经确认，需要决定页面结构、布局、组件组合和打开方式时使用。

## 应该怎么做

- 设计判断只使用已确认需求、正式页面范式、设计令牌、组件契约和真实示例。
- 历史规则与当前入口冲突时，以当前入口为准。
- 先确认页面职责和结构，再选择现有页面范式、组件与组合方式。
- 每个关键设计决定都要记录真实依据，方便实现和验收追溯。
- 找不到可靠依据时必须标记设计缺口，不能把判断甩给实现阶段。

## 不能怎么做

- 不能只凭视觉感觉拼组件。
- 不能临时发明页面范式、组件类或打开方式。
- 不能让内容多少改变主要区域的整体宽度和对齐。

## 完成后如何检查

当前正式沉淀的场景类型包括：组件消费决策、无 UI Kit 页面构成、对象管理列表构成、UI Kit 到生产转换、页面打开方式绑定、App 场景交付标准、App 宿主路径绑定、设计系统资源副本边界、wego-app 场景交付。检查每个页面都能说明使用了什么结构、为什么这样组合、如何打开，以及依据来自哪里。

<!-- generated-by: scripts/specs.mjs@3 -->
<!-- source-fingerprint: 5346c166c9b38892d9ddd057eefcd64431073276ed688595b169323d885ee073 -->
