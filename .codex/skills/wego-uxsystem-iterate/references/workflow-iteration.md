# 工作流迭代与经验沉淀

适用于用户明确要求“审查并沉淀经验”“补充规则”“复盘并形成经验”或“优化工作流”的场景。普通反馈、AI 自查、验收失败和实现偏差只用于完成当前任务，不自动进入经验池。

## 1. 入口门禁

进入本流程前必须同时满足：

1. 用户明确要求沉淀经验、补充规则、形成经验或优化工作流。
2. 当前任务属于工作流或设计系统规则回流，而不是普通业务实现。
3. 本轮能够选出一条最重要、最有复用价值的经验。
4. 能明确判断问题归属和未来权威落点。

任一条件不满足时，不更新 `experience/candidates.json`。

组件契约、Token、Preview、UI Kit 和样式的正常迭代继续按 `workflow.md` 执行，不受经验候选流程限制。

## 2. 经验候选录入

一次审查可能发现多个问题，经验池可以接收多条候选，但每条都必须完成归属判定、运行时可达性验证和场景拆分。

录入优先级：

1. 优先选择会重复影响多个业务场景的问题。
2. 优先选择最早产生错误、会向下游扩散的问题。
3. 优先选择已有权威来源无法正确约束的问题。
4. 同等情况下，选择对用户体验和工作流稳定性影响最大的一个。

不满足入口门禁、归属不明确或运行时可达性不完整的候选不得入池，只在当前 findings 和修复中处理。

## 3. 经验抽象漏斗

入池前依次完成：

1. **问题复盘**：只描述现象、影响和证据，不急着写规则。
2. **业务名词剥离**：把具体业务名替换成可复用的对象、页面、状态或流程。
3. **范式命名**：判断它属于需求理解、页面设计、组件消费、实现、验收还是工作流方法。
4. **反例验证**：至少换两个不同业务场景验证；换业务后失效的内容不能进入经验池。
5. **归并判断**：检查是否与已有候选、正式规则、pagePattern、fallback、组件契约或流程规则重复、补充、例外或冲突。

只服务当前页面的一次性问题，留在当前规格或实现，不进入经验池。

## 4. 归属判定

归属判断顺序固定为：

```text
问题在哪一步首次产生
→ 哪个技能本应做出正确决策
→ 正式规则应由哪个权威文件承载
→ 谁会在运行时消费
→ 谁负责最终验收
```

不得根据最后修改的文件、表面组件名称或最终暴露问题的位置判断归属。

### 4.1 wego-product

负责用户目标、页面范围、流程拆分、页面状态、信息块、业务规则和异常流程。

典型问题：需求理解错误、页面或状态遗漏、`page_spec` 信息不足。

### 4.2 wego-design

负责页面范式、UI Kit 匹配、布局、信息组织、组件选择组合和设计系统消费。

典型问题：页面范式错误、布局方向错误、组合约束未命中、`design_consumption_plan` 决策错误。

### 4.3 wego-ux

负责按已确认规格实现页面、宿主接入、DOM、CSS、交互和状态转换。

典型问题：规格正确但实现偏离、焦点切换、滚动、宽度、路由、交互状态或类名实现错误。

### 4.4 wego-tests

负责检查规格、设计计划和实现一致性，覆盖关键交互与状态，发现回归并正确归因。

典型问题：验收范围缺失、关键状态未覆盖、归因错误、自动化通过但真实规则未满足。

### 4.5 权威落点

- 单个组件结构、状态、变体和行为：`components/{slug}.json`，必要时同步对应 Preview。
- 页面范式、fallback、presentation 和多组件组合：`uikit-plan.json`。
- 设计系统读取、消费、复制和全局限制：`library-consumption.json`。
- Token 取值与语义：`colors_and_type.css` 与 `css.json`。
- 跨技能仓库硬约束：`AGENTS.md`。
- 技能触发条件、职责边界、必要输入、正式输出契约和跨技能交接：对应技能的 `SKILL.md`。
- 技能内部判断方法、详细步骤、检查清单、例外处理和案例：对应技能中由 `SKILL.md` 直接引用的 `references/*.md`。
- 可自动判断的问题：对应脚本、回归测试或守门规则。
- 经验候选：`experience/candidates.json`。

正式经验默认不得修改 `SKILL.md`。只有经验直接改变以下五项入口契约时，才允许修改对应技能的 `SKILL.md`：

1. 触发条件。
2. 职责边界。
3. 必要输入。
4. 输出契约。
5. 跨技能交接。

无法命中五项白名单时，禁止修改 `SKILL.md`。不得以“补充说明”“最佳实践”“注意事项”“新增检查项”或“经验沉淀”为由绕过该限制。

归属、权威落点、运行时消费或验收方式任一项不明确，本轮经验不得入池。

## 5. 候选匹配与计数

读取 `experience/candidates.json` 后执行：

1. 使用抽象后的 `problem_pattern`、主要归属、预期落点和触发条件匹配已有候选。
2. 业务名称不同但错误模式、判断条件和未来动作相同，视为同类。
3. 同类候选只增加 `occurrence_count`，并追加一条去重后的 `scene_evidence`。
4. 不同类问题才新增候选，初始次数为 1，并使用当前候选池 `threshold`；若已达到阈值，同轮置为 `awaiting-confirmation`。
5. 新增或更新时必须维护 `updated_at`；新候选同时维护 `created_at`。
6. 快速迭代阶段当前阈值为 1；标准稳定阶段阈值保留为 3，后续只调整候选池阈值与守门配置即可恢复。
7. 候选达到自身 `threshold` 时，状态改为 `awaiting-confirmation`，不得直接升级。
8. 历史 `observing` 候选不因阈值调整批量改为待确认；只有新增或本轮复用更新时，才按当前阈值进入确认。
9. 已处于 `awaiting-confirmation` 的候选再次出现时可以继续累计证据，但不得重复创建候选或绕过用户确认。

匹配不确定时不新增候选，先在最终风险中说明归属或去重仍需确认。

## 6. 阈值确认门禁

候选达到自身 `threshold` 时，必须向用户显示：

> 该经验已达到当前正式沉淀阈值（快速迭代阶段默认 1 次），是否现在将其升级为正式规则？

用户明确确认前：

- 不修改正式权威来源。
- 不新增 `scenarioTypeRegistry` 类型。
- 不把候选状态改为 `promoted`。
- 不生成假定已经生效的回归规则。

用户拒绝或暂不处理时，状态保持 `awaiting-confirmation`，继续保留证据。

## 7. 正式沉淀前的场景拆分

用户确认后，先写清：

- `applies_when`：哪些结构、语义、宿主或流程条件满足时适用。
- `avoid_when`：哪些相似场景不适用。
- `exceptions`：允许偏离的例外。
- `fallback`：条件不足、规则冲突或无法命中时如何回退。
- `merge_target`：是否应并入已有规则、补充触发条件或新增例外。
- `new_scenario_type_needed`：是否确实需要新增正式场景类型。

禁止把局部经验扩大成所有页面都必须遵守的一刀切规则。

## 8. 运行时可达性

正式规则必须完整说明：

1. 哪个技能会读取它。
2. 在流程的哪个时点命中。
3. 它影响哪个输出字段或实现动作。
4. 该决策如何传递给下游。
5. 最终由哪个验收项或守门脚本检查。

只有“写进文档”但没有消费和验收链路的规则，不得正式沉淀。

## 9. 正式沉淀步骤

1. 汇总达到当前阈值的证据和场景差异；快速迭代阶段允许 1 次证据进入用户确认，标准稳定阶段保留 3 次证据门槛。
2. 完成适用、不适用、例外和回退拆分。
3. 检查已有规则，优先合并、补充或增加例外。
4. 确定主要归属和次要归属。
5. 先执行 `SKILL.md` 五项白名单判断；未命中时不得修改 `SKILL.md`。
6. 将规则写入正确的结构化权威来源、reference、脚本或测试。
7. 只有成熟且会被运行时消费时，才新增或更新 `scenarioTypeRegistry`。
8. 更新对应技能的读取、输出或交接引用；只在入口契约确实变化时修改 `SKILL.md`。
9. 增加回归测试或守门。
10. 涉及设计系统本体时递增 `metadata.json.version`。
11. 运行 `node scripts/specs.mjs generate`。
12. 运行 `node scripts/specs.mjs check`、`node scripts/validate-skill-entry-boundary.mjs` 和 `node scripts/validate-wego-design.mjs`。
13. 人工检查生成文档是否准确表达规则。
14. 将候选状态改为 `promoted`，记录正式落点、确认时间和规则标识。

## 10. scenarioTypeRegistry 边界

`scenarioTypeRegistry` 只保存：

- 已成熟的正式场景类型。
- 明确的主要和次要工作流归属。
- 可执行的判断逻辑。
- 权威落点。
- 验收标准。

禁止保存：

- `occurrence_count`
- `scene_evidence`
- `observing`
- `awaiting-confirmation`
- 未经用户确认的候选
- 只服务单个业务页面的个案

## 11. 生成规则文档

`specs/*.md` 由 `scripts/specs.mjs` 从权威来源生成，只用于人工检查。

- 不直接编辑。
- 不被页面生成、设计消费、实现或验收技能读取。
- 修改权威来源后不要求实时生成，但提交前必须一致。
- 生成文档不能反向成为规则落点。

## 12. 最终检查

- 是否由用户明确触发经验沉淀。
- 是否正确去重和累计候选。
- 是否明确主要归属、次要归属和原因。
- 是否明确权威落点、消费方、输出字段、下游和验收方式。
- 达到当前阈值时是否仅提示确认。
- 未确认时是否没有修改正式规则。
- 正式沉淀前是否拆分适用、不适用、例外和回退。
- 是否先执行 `SKILL.md` 五项白名单判断。
- 未命中五项白名单时是否没有修改任何 `SKILL.md`。
- 是否没有把组件、页面、案例或详细检查规则追加到 `SKILL.md`。
- 是否没有把候选写入 `scenarioTypeRegistry`。
- 是否增加回归验证。
- 是否通过 `node scripts/validate-skill-entry-boundary.mjs`。
- 生成文档是否一致。
