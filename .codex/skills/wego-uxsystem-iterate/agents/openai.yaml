interface:
  display_name: "Wego UX 系统迭代"
  short_description: "按 wego-design 守门流程迭代组件、UI Kit、工作流"
  default_prompt: "Use $wego-uxsystem-iterate to read the wego-design library docs in order, iterate the component contract and preview safely, sync docs and extraction outputs when needed, bump metadata version for formal iterations, and always report changed items, verification, and remaining risks."

policy:
  allow_implicit_invocation: true
