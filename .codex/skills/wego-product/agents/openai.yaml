interface:
  display_name: "Wego Product"
  short_description: "创建并确认业务迭代范围，产出可追踪 interaction_spec"
  default_prompt: "Use $wego-product to create the business iteration, confirm its cross-scene scope, and produce traceable interaction_spec files before design or implementation."

policy:
  allow_implicit_invocation: true
