interface:
  display_name: "Wego Product"
  short_description: "理解新页面与业务变更，产出 interaction_spec"
  default_prompt: "Use $wego-product to turn this business request into a confirmed, traceable interaction_spec before design or implementation."

policy:
  allow_implicit_invocation: true
