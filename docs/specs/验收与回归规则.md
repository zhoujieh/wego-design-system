# 验收与回归规则

本文档由系统规则自动生成，用于人工检查。请勿直接修改；如需调整规则，应修改对应权威来源后重新生成。

## 什么时候使用

场景已经实现并注册路由，需要判断是否正确承接需求和设计时使用。

## 应该怎么做

- 验收必须一起检查需求、设计计划、正式规则依据和当前实现，不能只看截图或脚本结果。
- 每个关键设计和实现决定都要能追溯到正式规则来源。
- 问题必须归因到最早做错决定的环节，不能只看最后改了哪个文件。
- 除正常路径外，还要按需求检查空内容、长内容、重复操作、中断、失败、键盘焦点和返回恢复。
- 只记录真实执行过的脚本、浏览器检查、提交、推送和部署结果。

## 不能怎么做

- 不能只看自动化通过就认定体验正确。
- 不能只验证正常路径，忽略需求明确的边界和中断操作。
- 不能把实现偏差误归因到组件或最后修改的文件。

## 完成后如何检查

所有迭代需求编号都已覆盖，页面、信息、状态、异常流程、组件结构、打开方式、路由、反馈、键盘视口和资源同步均通过；通过后生成开发交接并冻结，问题有明确归属和复现依据。

<!-- generated-by: scripts/specs.mjs@7 -->
<!-- source-fingerprint: c969ce5df0f6922dad2d8316de1eee32d2fcc71657bd8342ec8310b84538fec6 -->
