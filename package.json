{
  "name": "wego-design-system",
  "private": true,
  "version": "0.0.0",
  "engines": {
    "node": ">=18"
  },
  "description": "微购设计系统与静态原型仓库(开发工具依赖)",
  "scripts": {
    "worktrees:stale": "node scripts/prune-worktrees.mjs",
    "worktrees:prune": "node scripts/prune-worktrees.mjs --force",
    "worktrees:test": "node scripts/prune-worktrees.mjs test",
    "branches:stale": "node scripts/prune-merged-branches.mjs",
    "branches:prune": "node scripts/prune-merged-branches.mjs --force",
    "branches:prune-gone": "node scripts/prune-merged-branches.mjs --force-gone",
    "branches:test": "node scripts/prune-merged-branches.mjs test"
  },
  "devDependencies": {
    "playwright": "^1.48.0"
  }
}
